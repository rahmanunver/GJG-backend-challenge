// TESTS ARE NOT WRITTEN, THIS SECTION IS NON-FUNCTIONAL

const request = require('jest');

const app = require('../app');

// submitScore()
describe('Testing of submitScore()', () => {
	test('Should post the submit score', () => {
		// const res = await request(app).post("/api/score/submit").send({
		//   score_worth: 100,
		//   user_id: uuid(),
		//   timestamp: 1098765432,
		// });
		// expect(res.statusCode).toEqual(201);
		// expect(res.body).toHaveProperty("user_id");
	});
});
